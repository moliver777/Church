<%= form_for(@news_article) do |f| %>
	<% if @news_article.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@news_article.errors.count, "error") %> prohibited this news ticker from being saved:</h2>
			<ul>
				<% @news_article.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<table>
		<tr>
			<th>Text</th>
			<td><%= f.text_field :title %></td>
		</tr>
		<tr>
			<th>Published?</th>
			<td><%= f.check_box :publish %></td>
		</tr>
	</table>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>